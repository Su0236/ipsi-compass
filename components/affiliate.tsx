export default function Affiliate(){const url=process.env.AFFILIATE_URL,disclosure=process.env.AFFILIATE_DISCLOSURE;if(!url||!disclosure||process.env.AFFILIATE_REVIEWED!=='true')return null;let parsed:URL;try{parsed=new URL(url)}catch{return null}if(parsed.protocol!=='https:'||!(parsed.hostname==='link.coupang.com'||parsed.hostname==='www.coupang.com'))return null;return <aside aria-label="광고" className="affiliate"><strong>광고 · 쿠팡 파트너스 제휴</strong><p>{disclosure}</p><a href={url} target="_blank" rel="sponsored noopener noreferrer">쿠팡 제휴 상품 보기</a></aside>}
